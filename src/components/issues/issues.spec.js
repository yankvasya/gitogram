import { mount } from '@vue/test-utils'
import issues from './issues'

describe('issues', () => {
  it('существует', () => {})

  it('mount', () => {
    // const wrapper = mount(issues)
  })
})
